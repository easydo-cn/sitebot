requests==2.22.0
gevent==1.1b6
# The version of Jinja after 2.8 uses the async/await syntax in Python3.
jinja2==2.8
# flask 1.0 has requirement Jinja2>=2.10.
Werkzeug==0.16.0
flask==0.12.2
# edo_client>=6.3.3  # please manually install from source
py-oauth2
setuptools==41.2.0
Fabric2
pycryptodome==3.4.7
netifaces
certifi
Flask-Babel==0.9
Babel==2.1.1
sh; sys_platform != 'win32'
python-dateutil
psutil==3.4.2
subprocess32; sys_platform != 'win32' and python_version < '3.0'
wincertstore; sys_platform == 'win32'
paho-mqtt==1.3.1
# PyOpenSSL 19.0.0 has requirement cryptography >= 2.3, whereas the latest version of
# cryptography is not compatible with WinXP, so we use the 2.3 version.
cryptography==2.3
pyopenssl==19.0.0
future
